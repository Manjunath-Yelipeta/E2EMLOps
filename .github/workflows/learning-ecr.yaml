name: Learning - ECR Pipeline

on:
  workflow_dispatch:

jobs:
  checkout-and-inspect:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        run: |
          echo "Current directory:"
          pwd

          echo "Repository files:"
          ls -la

